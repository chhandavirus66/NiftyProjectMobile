[project]
name = "nifty_app"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "flet",
    "yfinance",
    "pandas",
    "requests",
    "numpy"
]

[tool.flet.app]
path = "main.py"
product = "Nifty Pro Quant Terminal"
company = "Quant"
